rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /chats/{retailerId} {
      allow read, write: if request.auth != null;
    }
    match /chats/{retailerId}/messages/{messageId} {
      allow read, write: if request.auth != null;
    }
  }
}
